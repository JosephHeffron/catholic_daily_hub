export async function getApproxLocation(): Promise<{ city?: string; region?: string } | null> {
  // Placeholder: would use request IP or client-side geolocation. Server-side we return null.
  return null
}
